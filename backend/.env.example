TZ=UTC
PORT=3333
HOST=localhost
LOG_LEVEL=info
APP_NAME=YourAppName
APP_KEY=GENERATE_A_STRONG_KEY_HERE
NODE_ENV=development
SESSION_DRIVER=cookie

# Public URLs
APP_URL=http://localhost:3333
FRONTEND_URL=http://localhost:5173

# Database (replace with your credentials)
DATABASE_URL=postgresql://user:password@127.0.0.1:5432/app

# Redis (queue is optional)
REDIS_HOST=127.0.0.1
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_QUEUE=default

# Mail (use provider credentials)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USERNAME=your@email.com
SMTP_PASSWORD=

# Support contact used as fallback in errors
SUPPORT_EMAIL=support@yourdomain.com

LIMITER_STORE=redis

# Habilita o deshabilita el rate-limiter integrado.
# - true: activará los limiters definidos en `start/limiter.ts` (producción/QA).
# - false: devolverá middlewares NO-OP para facilitar desarrollo y pruebas locales.
RATE_LIMITING_ENABLED=true

# Notes:
# - Nunca pongas secretos reales en este archivo. Copia a `.env` y rellena valores privados.
# - Genera y guarda `APP_KEY` seguro para producción.